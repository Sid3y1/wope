<?php

/**
 * We put here all the configuration concerning the email addresses which will be used in the site.
 * 
 */

/**
 * Its the main address, this is from this address that all mail on the half on the site will be sent.
 */
$config['mailWebmaster']  = 'foo@example.com';

/**
 * Write here the address of the person responsible for the site.
 */
$config['mailResponsable']  = 'bar@example.com';

/**
 * This is at this address that all mails from the bug submit form will be sent.
 */ 
$config['mailBug'] = 'bug@exemple.com';

//$config['mailAsso']  = '';        //  Retro compatibilité

?>